version: '3'

services:
  gps:
    build: .
    command: uvicorn main:app --host 0.0.0.0
    volumes:
      - .:/gps
    ports:
      - "8008:8000"

  redis:
      image: redis:latest
      ports:
        - "6379:6379"
      environment:
        - REDIS_PORT=6379
        - REDIS_PASSWORD= 'kTfmjCzLLSgFnyw2OoGw'
  mongodb:
    image: mongo
    restart: always
    expose:
      - "27017"
    environment:
      - MONGO_INITDB_ROOT_USERNAME='linkar'
      - MONGO_INITDB_ROOT_PASSWORD='poiuytrewq'
